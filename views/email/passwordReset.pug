extends baseEmail

block content
    p Hi #{firstName},
        p Forgot your password? If yes then submit a PATCH request with your new password and passwordConfirm to: #{url}.
        p {the site for this function is not yet implemented}
        table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
                tr
                    td(align='left')
                        table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                            tbody
                                tr
                                    td
                                        a(href=`${utl}`, target='_blank') Reset your password
        p if you dint the Please ignore this message`